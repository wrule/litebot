import { Bot } from '.';
import { TC } from '../tc';

export
class SMACross
extends Bot<TC, TC, TC> {
  protected calculate(tc: TC, state_queue: TC[]) {
    return null as any;
  }

  protected analyze(state_queue: TC[]) {
    return null as any;
  }

  protected execute(signal: TC) {

  }
}
